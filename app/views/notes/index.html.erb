<%= render partial: 'components/header' %>

<%= render(
	partial: 'components/divs/hero_div',
	locals: {
		text: 'Read notes from all across the world',
		bg_url: 'hero_bg.svg',
		button: true,
		button_text: 'Read more',
		button_url: '#notes'
	}
) %>

<%= render(
	partial: 'components/graph_countries'
) %>

<%= render(
	partial: 'components/notes/notes_container',
	locals: {
		notes: @notes
	}
) %>
